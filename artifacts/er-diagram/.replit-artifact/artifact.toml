kind = "web"
previewPath = "/"
title = "ER Diagram Canvas"
version = "1.0.0"
id = "artifacts/er-diagram"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18708

[services.development]
run = "pnpm --filter @workspace/er-diagram run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/er-diagram", "run", "build" ]
publicDir = "artifacts/er-diagram/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18708"
BASE_PATH = "/"
